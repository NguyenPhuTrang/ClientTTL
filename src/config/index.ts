const routes = {
    home: '/',
    product: '/product',
    login: '/login',
    register: '/register',
    category: '/category/:id',
    notFoundPage: "/*",
};

const config = {
    routes,
};

export default config;
